# Queue — revision flowcharts

This folder currently has only `__init__.py` (no exercise `.py` files yet).

Deque / queue usage appears in **sliding window** (e.g. first negative in window). When you add queue exercises here, use the same **code → Mermaid** section layout as other topic flowchart docs.

**See also:** [SLIDING_WINDOW_FLOWCHARTS.md](../sliding_window/SLIDING_WINDOW_FLOWCHARTS.md)
